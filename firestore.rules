rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Most app data access goes through Firebase Admin on the server.
    // Keep client-side access denied by default until Firebase Auth is connected.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
